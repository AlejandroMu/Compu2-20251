const retriveData = (netId) => {

    console.error("Running bash commands directly from a browser is not possible due to security restrictions. Consider using a backend service to handle such operations securely.");
}
